@using MemberService.Pages.Event
@model EventInputModel

@{
    ViewData["Title"] = "Opprett nytt arrangement";
}
<h2>@ViewData["Title"]</h2>

<form method="POST" asp-action="Create" class="form-row justify-content-between">
    <partial name="_EditEvent" model="@Model" />
    <div class="col-md-12 mt-2">
        <button type="submit" class="btn btn-success">Opprett</button>
    </div>
</form>